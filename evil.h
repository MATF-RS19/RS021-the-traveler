#ifndef EVIL_H
#define EVIL_H

#include "building.h"
#include <QDebug>
#include <QString>
/*
class Evil: public Building
{
public:
    Evil(int x, int y, QString img)
        : Building(x, y, img)
    {
        this->setFlag(QGraphicsItem::ItemIsFocusable);
        this->setFocus();
    }

    void keyPressEvent(QKeyEvent *event);
    qDebug(QString * string);
};
*/
#endif // EVIL_H
